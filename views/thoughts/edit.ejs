<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <link href="https://fonts.googleapis.com/css2?family=Urbanist:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <title>Edit Thought | Shower Thoughts</title>
</head>
<body>
    <h1>Edit</h1>

    <form action="/thoughts/<%= thought._id %>?_method=PUT" method="POST">
        <div class="input-control">
            <label for="content">Thought</label>
            <textarea name="content" id="content"><%= thought.content %>
            </textarea>
        </div>
        <div class="input-control">
            <label for="categories">Category</label>
            <select name="categories" id="categories" multiple>
                <option value="Casual Thought" <%= thought.categories.includes('Casual Thought') ? 'selected' : '' %>>Casual Thought</option>
                <option value="Crazy Idea" <%= thought.categories.includes('Crazy Idea') ? 'selected' : '' %>>Crazy Idea</option>
                <option value="Speculation" <%= thought.categories.includes('Speculation') ? 'selected' : '' %>>Speculation</option>
                <option value="Musing" <%= thought.categories.includes('Musing') ? 'selected' : '' %>>Musing</option>
            </select>
        </div>

        <button type="submit">Submit</button>
    </form>
</body>
</html>